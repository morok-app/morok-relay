# Morok Relay — навантажувальний профіль

Заміряно 15 серпня 2026 на коміті після аудиту 4.
Відтворити: `redis-server --port 6399 --daemonize yes && python tools/load_profile.py`

Це четвертий клас питань зі статус-файлу («навантаження і вичерпання
ресурсів»), якого не дивився жоден із трьох попередніх аудитів. Тут
немає «правильно/неправильно» — тут цифри для планування заліза.

**Умови заміру:** одна нода Redis 7, локальна мережа, один процес Python.
На проді буде повільніше (мережа, конкуренція з Postgres/uvicorn), але
пропорції збережуться.

---

## Груповий fan-out

| Розмір групи | Медіана | На одного одержувача |
|---|---|---|
| 10 | 0.77 ms | 0.077 ms |
| 50 | 2.38 ms | 0.048 ms |
| 200 | 8.55 ms | 0.043 ms |
| 500 | 22.50 ms | 0.045 ms |

**Вартість на одержувача стала** (~0.045 ms) — саме те, чого домагались
фіксом HIGH-2. До нього було 2×N round-trip і зростання було б різким.

Розклад 22.5 ms для групи на 500:
* depth-check pipeline (1000 команд) — 9.7 ms
* fanout pipeline (1500 команд) — 13.8 ms

Це **чистий обсяг роботи**, а не втрачені round-trip. Оптимізувати
нічого: 2500 команд Redis фізично коштують стільки. Якщо колись
знадобиться швидше — єдиний реальний важіль це прибрати
`zremrangebyscore` з гарячого шляху (половина depth-check), поклавши
чистку протухлого повністю на reaper. Виграш ~20%, поки не потрібен.

---

## Пам'ять Redis

500 користувачів × 20 конвертів = 10 000 конвертів → **10.3 MB**

* **21.2 KB на користувача** (з 20 висячими конвертами)
* **1.1 KB на конверт** (meta + запис у inbox ZSET + tombstone)

Прогноз:

| Користувачів (по 20 конвертів) | Redis |
|---|---|
| 10 000 | ~207 MB |
| 100 000 | ~2.0 GB |

**Висновок по стелі:** виставлені `maxmemory` (relay1 = 2gb,
relay2 = 3gb) вистачає приблизно до **100 тисяч користувачів** з
двадцятьма недоставленими конвертами в кожного одночасно. Це дуже
далекий горизонт, але тепер він відомий, а не вгаданий.

Важливо: у Redis лежать **лише метадані**. Самі блоби — на диску
(`blob_storage`), тож дисковий профіль треба міряти окремо.

---

## Глибокий inbox

| Глибина черги | `list_inbox(limit=200)` |
|---|---|
| 10 | 0.36 ms |
| 100 | 1.07 ms |
| 1 000 | 1.95 ms |
| 5 000 (стеля) | 1.96 ms |

**Плоско після 1000.** Читання не деградує навіть на максимальній
глибині — MGET-pipeline у `list_inbox` працює правильно.

Це **знімає MEDIUM-1 з аудиту 4** як практичну проблему: побоювання
щодо вартості читань не підтвердилось. Рефакторинг `selectinload`
лишається питанням чистоти коду, а не продуктивності, і робити його
під приводом швидкодії не варто.

---

## Пропускна здатність запису

2000 конвертів у 50 паралельних потоках → **~2 940 конвертів/с**

Одна нода, один процес. Для месенджера на десятки тисяч користувачів
запас великий; вузьким місцем раніше стане мережа й Postgres.

---

## Tombstone'и (батч 4)

1000 конвертів → 1000 tombstone'ів, сумарно ~1 MB.

Tombstone живе довше за конверт (TTL конверта + 7 днів), тож саме він
визначає **нижню межу** пам'яті після сплеску трафіку: навіть коли всі
конверти доставлені й прибрані, сліди тримають пам'ять ще тиждень.
При 100 тис. конвертів за добу це ~100 MB, які «розсмоктуються» лише
через тиждень. У межах стелі, але варто пам'ятати.

---

## Що це змінює в беклозі

1. **MEDIUM-1 (`selectinload`) знято з пріоритетів** — виміряно, що
   проблеми немає.
2. **Стеля пам'яті обґрунтована** — 2gb/3gb вистачає до ~100k
   користувачів.
3. **Наступне вузьке місце — не Redis.** Далі має сенс міряти Postgres
   (групи, федеративні черги) і диск під блобами.
